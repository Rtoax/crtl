#ifndef __CRTL_EASY_OPERATOR_H
#define __CRTL_EASY_OPERATOR_H 1


#define and &&
#define or  ||

#define ABS(x) ((x) >= 0 ? (x) : -(x))
#define MIN(a,b) ((a) <= (b) ? (a) : (b))
#define MAX(a,b) ((a) >= (b) ? (a) : (b))

#endif //__CRTL_EASY_OPERATOR_H